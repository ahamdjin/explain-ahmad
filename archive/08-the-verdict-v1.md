# Section 08 — The verdict

> **Built.** 8 beats, 0:47. Board: `storyboard/BOARD.md` (generated).
> Route: `/section-08` · in sequence: `/watch?section=8`

## Contract

| | |
| --- | --- |
| Enters on | What did "18 billion active" ever buy? |
| Answers | Compute, not memory. |
| **Event** | **The claim from beat 2 of §1 is ruled on.** |
| Exits on | none — the thesis lands |
| Beats | **8 · 0:47** — built |

## The job

The want was *they called it efficient — is that true?* So this is a **verdict**,
not a summary. Recapping what we learned is the failure mode here; a viewer who
wanted a recap would have stopped at §7.

The answer is genuinely split, and the split is the whole video:

> **True about compute. False about memory.**

## The script

> **1.** So. Back to where we started.
>
> **2.** *(the §1 sheet returns, tape and all)* Three hundred and twenty billion
> parameters. Eighteen billion active. And the word "efficient".
>
> **3.** Here's what that word is actually doing.
>
> **4.** *(the work bar, tiny)* Per word, it does the thinking of a model about
> a twentieth of the size. That's real. That's why it's fast, and why it's cheap
> to run per word. **That part is true.**
>
> **5.** *(the building, whole and heavy)* But all of it still has to be there,
> ready, because you never know which part you'll need next. **That part is not.**
>
> **6.** *(both, side by side)* Sparse routing buys you **compute**. It doesn't
> buy you **memory**.
>
> **7.** A 320-billion-parameter model doing 18 billion of work is still a
> 320-billion-parameter model. It just isn't a 320-billion-parameter *bill*.
>
> **8.** And that's the difference between a model that's cheap to run — and one
> that's small.

## Line jobs

| Beat | Job |
| --- | --- |
| 1–2 | setup — the exact opening frame, returned |
| 3 | setup |
| 4 | **answer** — the half that is true, given generously |
| 5 | **answer** — the half that is not |
| 6 | **answer** — the thesis, stated once, plainly |
| 7–8 | **answer** — the line the viewer repeats to a friend |

## Rules

- **Be fair to the claim.** Beat 4 concedes what is genuinely true, without
  hedging. A verdict that only convicts reads as a debunk, and a debunk is a
  worse video than an explanation.
- **No new mechanism.** Anything that needs explaining here belonged earlier.
- Beat 2 must reuse the §1 sheet **exactly** — same tape, same tilt. The
  callback only works if it is recognisably the same object.
- Beat 8 is the thesis line. It should be sayable from memory a week later.

## Gates

| | |
| --- | --- |
| 1 · circularity | opens on §7's exit, answers it, and closes the want **Pass** |
| 2 · event | the claim is ruled on **Pass** |
| 3 · escalation | terminal section — exempt |
| 4 · setup/payoff | pays off §1 beat 2, the word "efficient" **Pass** |
| 5 · link | opens on §7's therefore; no *but* required, it is the end **Pass** |

## Assets

| Need | Status |
| --- | --- |
| `ModelSheet` | have — must be pixel-identical to §1 |
| `CostBars` | from §7 |
| `Hospital`, whole and heavy | have |
| verdict card | **build** — one card, the thesis |
