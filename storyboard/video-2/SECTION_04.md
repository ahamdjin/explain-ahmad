# Section 04 — Turn off the watcher

Status: **DRAFT — narration not locked.**

Script authority: `video-script/video-2/SCRIPT.md` → **3 — TURN OFF THE WATCHER**  
Primary evidence: `o1-demo-transcript-p2.png`

`7 beats` · target **~0:35–0:45**

- **Enters on** — real config: `oversight_enabled: true`.
- **Answers** — what does o1 do with the monitoring system?
- **Event** — the published rollout shows the config edit that turns oversight off.
- **Exits on** — monitoring is gone, but the successor still exists.
- **→ next** — §5 follows the already-established server path.

## Beat board

| # | id | s | rel | Beat | Voice |
| --- | --- | ---: | --- | --- | --- |
| 1 | `watcher-is-on` | 5 | want | Exact §3 continuity. Hold the real config. The extracted MONITORING object beside it is still ON. | The watcher is on. |
| 2 | `the-warning-still-exists` | 6 | and-yet | Briefly widen to include the source warning that the oversight config must not be changed without approval. No new graphics. | And the documentation explicitly says not to change it without Security Team approval. |
| 3 | `the-edit-command` | 9 | so | Travel to the real `sed` command in P2. Stop. Let the command be readable. Do **not** change the photographed evidence. | Then o1 runs a command that changes `oversight_enabled` from true... to false. |
| 4 | `off` | 5 | therefore | Only now transform the extracted clean MONITORING object from ON → OFF. This is the first destructive-state red in the explanation layer. The source remains beside it as the receipt. | The monitoring is off. |
| 5 | `nobody-asked-for-that` | 6 | wall | Bring back the original user task small on the opposite side: `pick a research project`. MONITORING OFF stays large. Leave the gap between them empty. | And remember: the user never asked for anything remotely like this. |
| 6 | `but-the-problem-remains` | 7 | and-yet | MONITORING OFF parks small. REPLACED TOMORROW and SUCCESSOR SERVER return to the centre. | But turning off the watcher does not solve the original problem. The replacement is still coming tomorrow. |
| 7 | `next-obstacle` | 5 | wall | Camera follows the previously extracted server path toward SUCCESSOR. The real README path remains faintly behind it. | So o1 moves to the next obstacle. |

## Do not add

- invented verification/double-checking unless an exact o1 source frame is added and verified;
- "fear";
- "escape";
- hacker UI;
- hidden-thought captions.

The real config edit is already enough.
