# The 18 Billion Mystery

**YouTube title:** **320B Parameters, Only 18B Active — Why Does It Need 8 GPUs?**

Production route: `/320b-parameters-only-18b-active-why-does-it-need-8-gpus`.
Compatibility routes `/watch` and `/video-1` redirect to it.

Code: `src/videos/glm-320b/video-1/`.
Storyboard and spine: `storyboard/video-1/`.

## Current canon

This folder contains the only active script for this video.

- §1 and §2 are explicitly marked **APPROVED NARRATION — LOCKED** in the current canon.
- §3–§13 are the current implemented narration and remain subject to author approval/reopening according to their script status.
- The tracked protagonist is **`it`**, token ID **432**.
- `READ_ALOUD.md` is the generated whole-film read-through.
- Rejected alternate scripts are not kept in the working tree. Git history is the archive.

## Production rule

Once narration is marked **APPROVED NARRATION — LOCKED**, the wording is immutable unless Ahmad explicitly reopens it. Beats, timing, components, camera moves and animation must adapt around the narration—not rewrite it.

Each numbered section contains narration plus its visual/story contract. The whole-film chain is defined in `storyboard/video-1/STORY_SPINE.md`.

## Implementation status

All 13 sections have executable production implementations under `src/videos/glm-320b/video-1/` and are covered by the repository verification gates. Script/build alignment, routes and the full 173-beat overlap scan passed on the productionized merge; future changes must rerun the gates rather than relying on that historical pass.
