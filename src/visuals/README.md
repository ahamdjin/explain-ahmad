# Reusable sketch visuals

Generic hand-drawn/sketch diagram primitives. They are available to the internal toolkit; the active film’s visual language is primarily governed by `src/paper/` and `art-direction/`.

Do not introduce a second visual language merely because a generic primitive exists here.
