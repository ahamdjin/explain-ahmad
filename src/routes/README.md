# Routes

Application-level pages and film navigation.

Production-facing pages include the landing page and `WatchPage`; lab/style/catalogue pages are internal tooling. The central film identity/path/chapter registry is `../videos/registry.tsx`.

Route changes must pass `npm run smoke`.
