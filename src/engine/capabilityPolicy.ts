export const capabilityPolicy = {
  defaultScroll: 'native',
  smoothScroll: 'opt-in',
  motion: 'scene-scoped',
  gsap: 'lazy',
  webgl: 'story-only',
  rive: 'story-only',
  lottie: 'story-only',
} as const
