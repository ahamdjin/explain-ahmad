# Playback / interaction engine

Reusable playback, scrolly, presenter, sequence and gesture infrastructure. This is implementation machinery, not story authority.

Before changing behavior used by production, inspect the active video routes and run the full smoke/visual checks. See `../../skills/ENGINE_CAPABILITIES.md` for the intended capability model.
