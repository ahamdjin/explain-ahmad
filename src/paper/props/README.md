# Paper props

Reusable physical props used inside the paper world. Props should encode a visual job that recurs; one-off decoration usually belongs in the relevant production stage instead.

Check `../../../art-direction/PROPS.md` and `../../../skills/ASSET_LIBRARY.md` before creating new assets. Preserve the handmade paper visual language and semantic scale.
