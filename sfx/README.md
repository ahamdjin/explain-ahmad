# SFX utility page

This folder contains the small browser-facing SFX utility/preview page (`index.html`). It is tooling, not part of the production film route and not the authoritative sound library.

Chosen production SFX and provenance live in `../assets/sfx/`. Sound workflow is documented in `../docs/SOUND.md`.
