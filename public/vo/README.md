# public/vo

Voice-over tracks go here, one per section: `01.mp3` … `08.mp3`.

Point a section at its track by setting `VO` in that section's `Section0N.tsx`.
See `docs/VOICE_OVER.md`. Nothing here is committed except this note.
